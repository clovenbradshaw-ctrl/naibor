# n8n credentials (used to log into the n8n UI at localhost:5678)
N8N_USER=admin
N8N_PASSWORD=changeme

# Webhook secret — n8n sends this to authenticate deploy requests
# Run: openssl rand -hex 32
WEBHOOK_SECRET=changeme
